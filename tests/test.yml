---
  - name: "test oah users"
    hosts: localhost
      #tasks:
      #- name: "add user"
      #  include: roles/oah-users/main.yml
    roles:
      - role: oah-users
        vars:
            oah_command: validate_user
            oah_user: test
            oah_user_group: admin
            oah_user_password: test
